# CLAUDE.md

Token 小精灵 —— macOS 桌面桌宠，用本机 AI 编程工具的真实 token 用量自动养大。

## 运行

macOS + Node.js 20+，仓库根目录：

```bash
npm install
npm start      # 编译 + 启动，桌宠出现在桌面右下角
```

打包 `.app`：`npm run pack`。测试：`npm test`。

完整说明见 [AGENTS.md](AGENTS.md)。
